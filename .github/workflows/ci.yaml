name: Build

on:
  push:
    branches: [main]
    paths-ignore:
      - 'deployment.yaml'

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # This is CRITICAL for git push
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}  # Add this
      
      - name: Login to Docker
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Generate tag
        run: |
          echo "VERSION=$(date +"%Y-%m-%d-%H%M%S")" >> $GITHUB_ENV
      
      - name: Build and push
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/flask-hello:${{ env.VERSION }} .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/flask-hello:${{ env.VERSION }}
      
      - name: Update deployment.yaml
        run: |
          echo "=== BEFORE ==="
          grep -n "image:" deployment.yaml
          sed -i "s|image:.*|image: ${{ secrets.DOCKERHUB_USERNAME }}/flask-hello:${{ env.VERSION }}|" deployment.yaml
          echo "=== AFTER ==="
          grep -n "image:" deployment.yaml
          echo "New image: ${{ secrets.DOCKERHUB_USERNAME }}/flask-hello:${{ env.VERSION }}"
      
      - name: Commit back
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git pull origin main --rebase
          git add deployment.yaml
          git commit -m "Update to ${{ env.VERSION }}"
          git push
